import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textoPrincipal: {
        fontFamily: "Poppins-Black",
        fontSize: 15,
        color: "#1a2b49",
        alignItems: "center",
        textAlign: "auto",
    }
})